In React, lifecycle methods are special methods in class components that allow you to run code at specific points during a component’s existence. The component lifecycle is divided into three main phases:

==>1. Mounting in React
# What is Mounting?
Mounting is the first phase in a React component's lifecycle when it gets created and inserted into the DOM (Document Object Model). This happens when a component is first rendered.

Mounting involves the following lifecycle methods in class components:

--->constructor()
--->static getDerivedStateFromProps()
--->render()
--->componentDidMount()
